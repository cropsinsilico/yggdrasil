---

model:
  name: rpcFibSrv
  driver: MatlabModelDriver
  args:
    - rpcFibSrv.m
    - "{{ FIB_SERVER_SLEEP_SECONDS }}"

  inputs:
    - name: srv_fib
      driver: RMQServerDriver
      args: srv_fib   # connected to this queue, shared by all models 

