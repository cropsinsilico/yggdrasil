chevron
flask
jsonschema==3.2.0
matplotlib
numpy>=1.13.0
pandas
pyperf
psutil
python-rapidjson
pyyaml
pyzmq
requests
scipy
# scipy; platform_system == 'Darwin'
# scipy==1.7.1; platform_system != 'Darwin'
six
sympy
sysv_ipc; platform_system != 'Windows'
unyt
GitPython
# typing_extensions==4.0.0; platform_system == 'Linux'
# charset-normalizer==2.0.7; platform_system == 'Linux'
# fonttools==4.28.1; platform_system == 'Linux'
# setuptools==59.2.0; platform_system == 'Linux'
